## What changed

<!-- Outcomes, not steps. One line per real change. -->

## Why

<!-- The problem this fixes. If it fixes a drift, name what drifted from what. -->

## Standards Check

<!-- Required. `pass` / `partial` / `fail`, citing the charter rules that applied.
     A close with no Standards Check is itself a fail under Rule 1. -->

- Result: `pass` / `partial` / `fail`
- Rules that applied:
- Rules deliberately not met, and why:

## Verification

<!-- The command you ran and its output. Not "tested locally" -- the actual output.
     Do not trust a brief, a comment, or a count; verify against disk or the live API. -->

```
```
