# Every path in this repo requires review from the owner below.
#
# Canonical: standards/repo-baseline/files/.github/CODEOWNERS in
# cfoforit-claude-plugins. Do not edit in place.
#
# This file is `templated`, not `exact`: /tools/
/plugins/ is substituted per repo with
# that repo's own top-level directories. The OWNER is asserted separately by the
# manifest's `owner_must_be`, so a well-formed file that names the wrong approver is a
# distinct and higher-severity finding than a formatting difference.

*                       @Storres1970
/.github/               @Storres1970
/tools/
/plugins/
